# Database (use these for AWS RDS; leave unset for local default)
# DB_HOST=your-rds-endpoint.region.rds.amazonaws.com
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
# DB_NAME=levelup

# AWS SQS (set only when deploying to AWS for decoupled processing)
# AWS_REGION=us-east-1
# SQS_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/123456789012/joystick-store-events

# AWS credentials (optional if using IAM role on EC2/ECS/Lambda)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
